# To build, run mvn clean install -DskipTests
# To generate ddl, append exec:java -DmainClass=com.easybill.config.db.GenerateDDL to maven goal.
# To install lombok, download lombok jar and install it to IDE location by double clicking on it.Then, restart IDE.
